traces:
  - name: time-to-triage
    model: ref(issues)
    columns: 
      time_to_triage: avg( datediff('hour', STARTED_TRIAGE_AT, TRIAGED_AT ) )
    props:
      type: indicator 
      value: column(time_to_triage)[0]
  - name: time-to-triage
    model: ref(issues)
    columns: 
      time_to_triage: avg( datediff('hour', STARTED_TRIAGE_AT, TRIAGED_AT ) )
    props:
      type: indicator 
      value: column(time_to_triage)[0]